# بناء PhysioCenter Setup.exe على Windows

تم تجهيز الحزمة بهوية PhysioCenter:
- اسم البرنامج: **PhysioCenter**
- ملف التثبيت: **PhysioCenter-Setup-1.0.0.exe**
- أيقونة مخصصة للبرنامج والاختصارات.
- شاشة تثبيت NSIS مخصصة باسم PhysioCenter وبهوية بصرية طبية.
- اختصار على سطح المكتب + قائمة Start.

## المتطلبات على جهاز البناء فقط
- Windows 10/11 64-bit
- Node.js LTS
- اتصال بالإنترنت أثناء أول Build لتنزيل الحزم المطلوبة.

## الطريقة
1. فك ضغط الملف.
2. افتح المجلد.
3. شغّل `BUILD-WINDOWS.bat`.
4. بعد انتهاء البناء ستجد داخل `dist` ملف:
   `PhysioCenter-Setup-1.0.0.exe`

## على أجهزة المستخدمين
المستخدم النهائي لا يحتاج Node.js أو npm أو اتصال إنترنت لتشغيل البرنامج. يحتاج فقط Windows 64-bit ويشغل ملف الـSetup.

## بيانات التجربة
- doctor / 1234
- center / 1234

قبل التوزيع التجاري غيّر كلمات المرور التجريبية ووقّع ملف التثبيت بشهادة Code Signing إن أمكن.
