let patients=[]; async function load(){patients=await window.api.getPatients();render()} function render(){let q=(document.getElementById('q').value||'').toLowerCase();document.getElementById('list').innerHTML=patients.filter(p=>`${p.id} ${p.full_name} ${p.phone||''}`.toLowerCase().includes(q)).map(p=>`<div class="card" onclick="location.href='patient.html?id=${p.id}'"><h3>${p.full_name}</h3><p>رقم الملف: ${String(p.id).padStart(5,'0')}</p><p>${p.phone||'بدون هاتف'}</p><p>${p.diagnosis||'بدون تشخيص'}</p></div>`).join('')}load();